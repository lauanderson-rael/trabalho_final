<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Coworking</title>
    <!-- 
    <link rel="stylesheet" href="css/bootstrap.min.css">
    -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body class="bg-dark-subtlezzz" style="background-color: #6a7879; color: #fff ">

    <nav class="navbar" style="background-color: #07767b;">
        <div class="container-fluid">
            <span class="navbar-brand text-white mb-0 h1">
                <img src="/img/logo.png" alt="logo image" width="70px">
                <a href="/" class="text-decoration-none text-white text-hover">Sistema de Reservas de Coworking</a>
            </span>

            {{#if usuario}}

                <div class="dropdown">
                    <button class="btn btn-outline-light dropdown-toggle" type="button" data-bs-toggle="dropdown"
                        aria-expanded="false">
                        <i class="fa-solid fa-user"></i> Usuário
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="/usuario/dados/{{usuario.id}}"><i class="fa-regular fa-id-card"></i> Meus Dados</a></li>

                        <div class="dropdown-divider"></div>
                        <li>
                            <a class="dropdown-item" href="/usuario/logout"><i class="fa-solid fa-right-from-bracket"></i> Sair</a>
                        </li>
                    </ul>
                </div>
            {{else}}
                <a href="/usuario/login" class="btn btn-light">LOGIN</a>
            {{/if}}


        </div>
    </nav>
    {{#if success_msg}}
      <div class="alert alert-success" role="alert">
        {{success_msg}}
      </div>
    {{/if}}

    {{#if error_msg}}
      <div class="alert alert-danger" role="alert">
        {{error_msg}}
      </div>
    {{/if}}

    {{#if error}}
      <div class="alert alert-danger" role="alert">
        {{error}}
      </div>
    {{/if}}

    {{{body}}}

    <script src="/js/bootstrap.bundle.js"></script>
</body>

</html>
